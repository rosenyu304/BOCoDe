# Problems excluded from the campaign — from experiments/problem_audit.py.
# EXCLUDE ONLY on structural evidence that the problem cannot be optimized:
#   (a) a constraint is CONSTANT;
#   (b) a constraint is PROVABLY never satisfiable (few distinct values, none <= 0)
#       -> the feasible set is EMPTY;
#   (c) the OBJECTIVE is near-discrete -> nothing for a GP to model;
#   (d) the problem ERRORS on evaluation.
#
# DO NOT exclude for:
#   * a single COARSE constraint among smooth ones -- that is HARD, not broken;
#   * 'no feasible point found by random sampling' -- that is a TINY feasible region, and
#     finding it is the entire point of constrained BO.
#
# CEC2020_p20 was REMOVED from this list on 2026-07-14: it was never unsolvable, it was
# CORRUPTED by a mis-applied 0.0625 gauge snap that collapsed both truss areas to {0, 0.0625}
# (an area of 0 -> divide-by-zero -> inf -> the 1e6 sentinel -> step-function constraints).
# With the snap removed it reproduces its published optimum 263.895843 exactly.
#
# An earlier version of this rule fired on any single low-cardinality constraint and would
# have deleted all 15 MODAct problems (7 of their 8 constraints are smooth and they DO have
# feasible points) -- i.e. more than half of Table 4, silently. Caught and reverted.

# ===========================================================================
# THE EXCLUSION LIST IS NOW EMPTY. Every problem previously excluded was FIXED:
#   CEC2020_p20  — was 'unsolvable'. It was CORRUPTED by a mis-applied 0.0625 gauge snap
#                  (areas collapsed to {0, 0.0625}; area 0 -> divide-by-zero -> inf -> the
#                  1e6 sentinel -> step-function constraints). Snap removed; it now
#                  reproduces its published optimum 263.895843.
#   Truss120D    — called super().scale(X) UNCONDITIONALLY, so it rejected the already-scaled
#   Truss200D      X that every other problem receives -> RangeException on every evaluation.
#                  Scaling is now opt-in, matching the working sibling Truss72D.
#
# An exclusion is a scientific claim: it removes a benchmark from the paper. Meet the same
# evidentiary bar as a result — root cause identified, fix derived from the source, published
# optimum reproduced. 'It crashes' is a reason to investigate, not to exclude.

# ===========================================================================
# THE TWO EXCLUSIONS — and their causes, which are NOT the same.
# The recorded CAUSE is part of the claim: get the exclusion right but the reason
# wrong, and a FIXABLE BUG becomes permanent because a future reader believes the
# benchmark is inherently useless.

InvertedPendulumProblem
# DEGENERATE BY CONSTRUCTION — not a bug, and not repairable.
# These MuJoCo *Problem variants optimise a single CONSTANT ACTION. No constant torque
# can balance a pole, so the episode always terminates after one step and the reward is
# always exactly 1.0. There is nothing to optimise and nothing to fix: the only "fix"
# would be adding a policy-search variant, which is a NEW problem, not a repair.
# (Checked all 9 constant-action MuJoCo problems — this is the ONLY affected one.
#  InvertedDoublePendulumProblem is also dim-1 but varies normally.)

LassoRCV1
# 🐛 BUG — NOT a "constant objective" problem. The DESIGN VECTOR NEVER REACHES THE MODEL.
# Measured: f(all-0) = f(all-mid) = f(all-1) = -0.0262927778 exactly (spread 0.0), while
# every sibling responds normally (LassoDNA 5.3e-1, LassoLeukemia 2.3e-1,
# LassoBreastCancer 1.9e-1, LassoDiabetes 7.2e-2). So the LassoBench integration works;
# the RCV1 instance specifically does not.
#
# ROOT CAUSE (proven): _lasso_base.py:128 hard-codes `Lasso(alpha=1.0)`. RCV1's targets
# have std 0.175 and its TF-IDF features are tiny, so alpha=1.0 is **1258x alpha_max**
# (the penalty above which EVERY coefficient is shrunk to exactly zero). All 47,236
# coefficients die, the prediction collapses to the intercept, and the objective is then
# mathematically independent of the design vector. Coefficients only survive below
# alpha ~ 1e-4.
#   alpha=1e0..1e-3 -> 0 nonzero coefs, val MSE 0.0262927778 (the observed constant)
#   alpha=1e-4      -> 131 nonzero coefs, val MSE 0.0237598784
#
# THE FIX (ready, NOT applied — it is a science decision for Rosen):
#   make the penalty DATASET-RELATIVE, alpha = frac * alpha_max (what LassoBench itself
#   does), instead of a hard-coded 1.0. NB alpha=1.0 is above alpha_max for the OTHER
#   datasets too (Diabetes 4.6x, BreastCancer 2.5x) -- they only escape because the
#   feature rescaling has enough leverage there. So the fix CHANGES THE OBJECTIVE FOR ALL
#   5 LASSO PROBLEMS and their existing results would have to be re-run. That is why it
#   was not applied unilaterally.
